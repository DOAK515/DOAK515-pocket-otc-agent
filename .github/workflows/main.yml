name: Continuous Trading Bot 24/7

on:
  schedule:
    - cron: '*/1 * * * *'  # يتم إعادة تشغيل السكربت تلقائياً كل دقيقة لضمان العمل المستمر 24/7
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install requests pandas numpy pytz matplotlib

      - name: Run bot script
        run: |
          python bot.py
